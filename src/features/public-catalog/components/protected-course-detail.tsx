import Image from "next/image";
import Link from "next/link";
import { Clock3Icon, LockIcon, PlayCircleIcon } from "lucide-react";

import { buttonVariants } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import type { CatalogCourse, CatalogTeacher } from "@/features/public-catalog/types/catalog";
import { cn } from "@/lib/utils";

const packageLabel = { SINGLE_LESSON: "باقة حصة", MONTHLY: "باقة شهر", TERM: "باقة ترم", FINAL_REVISION: "مراجعة نهائية", CUSTOM: "باقة مخصصة" } as const;

export function ProtectedCourseDetail({ course, teacher }: { course: CatalogCourse; teacher: CatalogTeacher }) {
  return <main className="mx-auto w-full max-w-7xl px-4 py-6 sm:px-6 sm:py-8 lg:px-8"><section className="grid gap-5 rounded-xl border bg-card p-4 sm:p-5 lg:grid-cols-[minmax(0,1fr)_22rem]"><div className="order-2 lg:order-1"><p className="text-sm font-semibold text-primary">{course.subject} · {course.grade}</p><h1 className="mt-2 text-2xl font-bold leading-tight sm:text-4xl">{course.title}</h1><p className="mt-3 max-w-2xl text-sm leading-7 text-muted-foreground">{course.description}</p><Link href={`/teachers/${teacher.id}`} className="mt-4 inline-flex rounded-lg bg-secondary px-3 py-2 text-sm font-medium transition hover:bg-secondary/70 focus-visible:outline-none focus-visible:ring-3 focus-visible:ring-ring/50 active:scale-[.98]">{teacher.name} · {teacher.subject}</Link></div><div className="relative order-1 aspect-video overflow-hidden rounded-lg bg-secondary lg:order-2"><Image src={course.cover} alt={course.coverAlt} fill priority sizes="(max-width: 1024px) 100vw, 352px" className="object-cover" unoptimized /></div></section><div className="mt-6 grid gap-6 lg:grid-cols-[minmax(0,1fr)_22rem]"><section><h2 className="text-xl font-bold sm:text-2xl">محتوى الكورس</h2><p className="mt-1 text-sm text-muted-foreground">كل درس يفتح داخل المنصة. صورة الدرس وعنوانه لا ينقلانك إلى YouTube.</p><div className="mt-4 space-y-4">{course.units.map((unit) => <Card key={unit.id}><CardHeader><CardTitle>{unit.title}</CardTitle></CardHeader><CardContent className="grid gap-2">{unit.lessons.map((lesson) => <Link key={lesson.id} href={`/courses/${course.id}/lessons/${lesson.id}`} className="group flex items-center gap-3 rounded-xl border bg-background p-2.5 transition-all hover:border-primary/40 hover:bg-secondary/30 focus-visible:outline-none focus-visible:ring-3 focus-visible:ring-ring/50 active:scale-[.995]"><div className="relative aspect-video w-24 shrink-0 overflow-hidden rounded-lg bg-secondary sm:w-32"><Image src={lesson.thumbnailUrl ?? course.cover} alt={`صورة درس ${lesson.title}`} fill sizes="128px" className="object-cover transition-transform group-hover:scale-105" unoptimized /><span className="absolute inset-0 flex items-center justify-center bg-foreground/15"><PlayCircleIcon className="size-7 text-white drop-shadow" /></span></div><div className="min-w-0 flex-1"><h3 className="line-clamp-2 text-sm font-semibold sm:text-base">{lesson.title}</h3><p className="mt-1 flex items-center gap-1 text-xs text-muted-foreground"><Clock3Icon className="size-3.5" />{lesson.duration}{lesson.preview ? " · معاينة مجانية" : " · يتطلب وصولًا"}</p></div>{!lesson.preview ? <LockIcon className="size-4 shrink-0 text-muted-foreground" /> : null}</Link>)}</CardContent></Card>)}</div></section><aside><h2 className="text-xl font-bold">اختر الباقة</h2><div className="mt-4 space-y-3">{course.packages.map((item) => <Card key={item.id} className={item.type === "TERM" ? "ring-primary/35" : ""}><div className="relative aspect-[16/7] bg-secondary"><Image src={item.thumbnailUrl ?? course.cover} alt={item.title} fill sizes="352px" className="object-cover" unoptimized /></div><CardContent><p className="text-xs font-medium text-primary">{packageLabel[item.type]}</p><h3 className="mt-1 font-semibold">{item.title}</h3><p className="mt-2 text-sm text-muted-foreground">{item.scope} · {item.duration}</p><strong className="mt-3 block text-lg">{item.price === 0 ? "مجاني" : `${item.price} ج.م`}</strong><Link href={`/payments/new?offer=${item.id}&course=${course.id}`} className={cn(buttonVariants(), "mt-3 w-full active:scale-[.98]")}>اختيار الباقة</Link></CardContent></Card>)}</div></aside></div></main>;
}
